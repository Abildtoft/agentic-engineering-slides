node_modules
.DS_Store
dist
*.local
.context
.env
.pnp.*
**/.yarn/install-state.gz
.gstack/
.vercel

# Rendered narration clips (mascot .mp4s) are tracked through Git LFS — see
# .gitattributes.
#
# The ElevenLabs speech .mp3s stay out: cheap to re-synthesise (seeded, so the
# take barely moves), and superseded as the shipped asset the moment the slide's
# mascot clip renders from them.
public/narration/*.mp3
