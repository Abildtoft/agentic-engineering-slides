node_modules
.DS_Store
dist
*.local
.context
.env
.pnp.*
.yarn/install-state.gz
.gstack/

# Rendered avatar clips are tracked through Git LFS — see .gitattributes.
#
# Audio-mode speech stays out: it is the cheap rehearsal artefact, re-synthesised
# on every prose edit, and worth nothing once the video exists.
public/narration/*.mp3
